export interface Product {
    id: number;
    title: string;
    description: string;
    price: number;
    thumbnail: string;   // DummyJSON usa 'thumbnail' invece di 'image'
    brand: string;
    category: string;
}